.werewolves_phase
  .choose_stage
    Pick your target!
    - if @game.werewolf_leader == @player
      - if @player.response == nil
        - @game.players.reject{|p| p.role == "werewolf" || !p.alive}.each do |player|
          .vote.button{player_id: player.id}
            = player.name

  .confirm_stage
    You are about to kill
    %span.target_name
    .comfirm.button
      Kill
    .cancel.button
      Cancel
